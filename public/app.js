/** Todo web app behaviour — stubs to be implemented. */

/**
 * Wire the form, the error line and the list on the page.
 * @param {Document} doc
 */
export function initTodoApp(doc) {
  throw new Error('not implemented');
}

initTodoApp(document);
