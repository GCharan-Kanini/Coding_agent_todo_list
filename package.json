{
  "name": "todo-landing",
  "version": "0.1.0",
  "private": true,
  "description": "Responsive static landing page for a Todo application (React + Vite)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npx vitest run --environment jsdom"
  },
  "vitest": {
    "test": {
      "environment": "jsdom"
    }
  },
  "dependencies": {
    "@testing-library/react": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.0.0",
    "@vitejs/plugin-react": "^4.7.0",
    "jsdom": "^22.1.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
