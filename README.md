# Coding agent todo list (Node / vitest)

Seed branch for the Propel Coding Agent multi-language validation. `src/todo.js` holds
importable stubs; the agent must implement them and the Unit Testing Agent writes vitest
tests under `tests/`.
